You are to keep researching until the morning. Your job is to create a better alternative to LRU or LFU or whatever that gets the best speed up across all sorts of settings on this new project. You must not reward hack. That means you can't find tricks or special settings where it works. Your job is to genuinely make a model that just works and is much better than LRU. The new insight is that we now have access to the actual text, so we can predict how long the output is going to be, or predict if it's coming in a batch, or do some sort of predictive analysis on it. The model you train should be tiny so it runs fast. Your job is to constantly think about making the best model possible that achieves the actual goal, not a specialized version of the goal. Every once in a while, you should check to make sure you're not reward hacking. The overall goal is to make a small model that can quickly decide where the KV should be kept, how much of the KV should be kept, etc. Just a better alternative. You must continue in a loop for the next 12 hours autonomously; do not stop because I'm going to sleep. 

this is an autoresearch bot